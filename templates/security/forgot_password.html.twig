{% extends 'base.html.twig' %}

{% block title %}Mot de passe oublié{% endblock %}

{% block body %}
<div class="container py-3 py-md-4" style="max-width: 520px;">
  <div class="mb-3">
    <h1 class="h3 mb-1">Mot de passe oublié</h1>
    <div class="text-body-secondary">
      Entre ton email et on t’enverra un lien de réinitialisation.
    </div>
  </div>

  <form method="post" class="card" data-turbo="false">
    <div class="card-body">
      <div class="mb-3">
        <label for="email" class="form-label">Email</label>
        <input
          type="email"
          id="email"
          name="email"
          class="form-control"
          autocomplete="email"
          inputmode="email"
          required
        >
      </div>

      <div class="d-grid gap-2">
        <button type="submit" class="btn btn-primary btn-lg">
          Envoyer le lien
        </button>
        <a href="{{ path('app_login') }}" class="btn btn-link">
          Retour à la connexion
        </a>
      </div>
    </div>
  </form>

  <div class="text-body-secondary small mt-3">
    Si un compte existe pour cet email, tu recevras un message (pense à vérifier les spams).
  </div>
</div>
{% endblock %}
