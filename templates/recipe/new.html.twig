{% extends 'base.html.twig' %}

{% block title %}Nouvelle recette{% endblock %}

{% block body %}
  <div class="card shadow-sm">
    <div class="card-body">
      <h1 class="h4 mb-4">➕ Nouvelle recette</h1>

      {{ include('recipe/_form.html.twig', { button_label: 'Créer' }) }}
    </div>
  </div>
{% endblock %}
