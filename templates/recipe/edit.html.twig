{% extends 'base.html.twig' %}

{% block title %}Modifier recette{% endblock %}

{% block body %}
  <div class="card shadow-sm">
    <div class="card-body">
      <h1 class="h4 mb-4">✏️ Modifier la recette</h1>

      {{ include('recipe/_form.html.twig', { button_label: 'Mettre à jour' }) }}
    </div>
  </div>
{% endblock %}
