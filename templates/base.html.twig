<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="UTF-8">
    <title>{% block title %}Receiplan{% endblock %}</title>

    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    >

    {% block stylesheets %}{% endblock %}

    {% block javascripts %}
      {{ importmap('app') }}
    {% endblock %}
  </head>

  <body>
    <!-- Navbar mobile -->
    <nav class="navbar navbar-dark bg-dark d-lg-none">
      <div class="container-fluid">
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="offcanvas"
          data-bs-target="#sidebar"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <span class="navbar-brand mb-0 h1">🍽️ Receiplan</span>
      </div>
    </nav>

    <div class="d-flex">
      <!-- Sidebar -->
<div
  class="offcanvas-lg offcanvas-start bg-dark text-white"
  tabindex="-1"
  id="sidebar"
>
  <div class="offcanvas-header d-lg-none">
    <h5 class="offcanvas-title">🍽️ Receiplan</h5>
    <button
      type="button"
      class="btn-close btn-close-white"
      data-bs-dismiss="offcanvas"
    ></button>
  </div>

  <div class="offcanvas-body p-0">
    <nav class="nav flex-column p-3">

      <a
        class="nav-link text-white {% if app.request.attributes.get('_route') starts with 'app_recipe' %}active fw-bold{% endif %}"
        href="{{ path('app_recipe_index') }}"
      >
        📖 Recettes
      </a>

      <a
        class="nav-link text-white {% if app.request.attributes.get('_route') starts with 'app_ingredient' %}active fw-bold{% endif %}"
        href="{{ path('app_ingredient_index') }}"
      >
        🧂 Ingrédients
      </a>

      <a
        class="nav-link text-white {% if app.request.attributes.get('_route') starts with 'stock_' %}active fw-bold{% endif %}"
        href="{{ path('stock_index') }}"
      >
        🧺 Stock
      </a>

      {# 👉 Nouveau lien Planning #}
      <a
        class="nav-link text-white {% if app.request.attributes.get('_route') starts with 'meal_plan' %}active fw-bold{% endif %}"
        href="{{ path('meal_plan_index') }}"
      >
        📅 Planning
      </a>

      <hr class="border-secondary my-2">

      <a
        class="nav-link text-white {% if app.request.attributes.get('_route') starts with 'voice_' %}active fw-bold{% endif %}"
        href="{{ path('voice_index') }}"
      >
        🎙️🧠 Assistant
      </a>

    </nav>
  </div>

</div>


      <!-- Main content -->
      <main class="flex-fill p-4 bg-light min-vh-100">
        {% block body %}{% endblock %}
      </main>
    </div>

    <!-- Bootstrap JS -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    ></script>
  </body>
</html>
